# ESP32 E-Ink Messenger Firmware

## Flashing Instructions

1. Install MicroPython on your ESP32 device.
2. Copy files from `src/` to the device.
3. Configure WiFi and API settings in `config.py`.
4. Run `main.py` to start the firmware.

## Setup Requirements
See `requirements.txt` for MicroPython libraries.
